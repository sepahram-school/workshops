# Plan: MinIO Distributed Cluster + Re-benchmark

## Changes Required

### 1. `docker-compose-minio.yml` — Convert to 3-node distributed cluster
Replace single-node with 3-node distributed MinIO (matching user's provided config).
Key adaptations from user's config:
- Use port 9200 (to avoid RustFS port 9000 conflict)
- Use volume mounts (`./data-minioN:/data`) instead of named volumes (consistent with other compose files)
- Same credentials (minioadmin/minioadmin123)
- All 3 nodes share `MINIO_VOLUMES: "http://minio{1...3}:9000/data"`

### 2. `run-benchmark.ps1` — Fix default sizes
Change line 16: `$Sizes = "1mb,32mb,128mb"` → `$Sizes = "1mb,16mb,32mb"`

### 3. `results/` — Clear all benchmark results
Delete all 108 JSON files (3 modes × 3 sizes × 4 targets × 3 runs)

### 4. Run all benchmarks
Execute: `uv run python benchmark.py --target all --sizes 1mb,16mb,32mb --runs 3 --mode heavy` (and repeat for write-only, read-only)

### 5. `README.md` — Update with new results
- Recalculate means/stdevs from new JSON data
- Update all throughput and latency tables
- Fix SeaweedFS container count (9 → 8)
- Update MinIO description (single-node → 3-node distributed)
- Update conclusion that MinIO now runs as distributed cluster

## Verification
- `docker compose -f docker-compose-minio.yml up -d` should start 3 MinIO containers
- MinIO Console accessible at localhost:9201
- S3 API at localhost:9200
- All benchmark runs complete with 100% success rate
- README numbers match the new JSON results
